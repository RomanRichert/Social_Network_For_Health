spring:
  main:
    banner-mode: off

  datasource:
    url: ${jdbc:mysql://localhost:3306/ok_treatments}
    username: root
    password: bigmir1704
#    url: ${datasource.url}
#    username: ${datasource.username}
#    password: ${datasource.password}

  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    sql:
      init:
        mode: always


  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
    enabled: true

app:
  description: ${app.description}
  version: ${app.version}
  server:
    url: ${app.server.url}

management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"

info:
  name: bank
  description: ${app.description}